# ---------------------------------------------------------------- web layer
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.6,<3.0
pydantic-settings>=2.2
python-multipart>=0.0.9

# ------------------------------------------------------------------- data
pandas>=2.1
numpy>=1.26
scipy>=1.11
pyarrow>=15.0            # parquet disk cache
yfinance>=0.2.40
requests>=2.31
PyYAML>=6.0              # RL hyperparameter configs in configs/

# --------------------------------------------------------- authentication
bcrypt>=4.1              # password hashing (deliberately slow by design)
PyJWT>=2.8               # signed session tokens

# ------------------------------------------------------------ AI assistant
# Async HTTP client for the Hugging Face-backed chat. Runtime, not dev-only:
# app/services/chat/provider.py imports it on every request.
httpx>=0.27

# ------------------------------------------------------- machine learning
scikit-learn>=1.4
torch>=2.2               # CPU wheel is fine: pip install torch --index-url https://download.pytorch.org/whl/cpu

# --------------------------------------------------- reinforcement learning
gymnasium>=0.29
stable-baselines3>=2.2   # optional but recommended (PPO / A2C / SAC / TD3 / DDPG)
sb3-contrib>=2.2         # optional: QR-DQN and TRPO

# -------------------------------------------------------------- persistence
SQLAlchemy>=2.0
aiosqlite>=0.20
# psycopg[binary]>=3.1   # uncomment for PostgreSQL
# redis>=5.0             # uncomment for a shared cache
# pymongo>=4.6           # uncomment to archive raw news documents

# ------------------------------------------------- advanced quantitative
statsmodels>=0.14        # ARIMA/GARCH diagnostics, Ljung-Box, ADF
arch>=6.3                # GARCH / EGARCH / GJR volatility models

# ----------------------------------------------------------------- workers
APScheduler>=3.10

# ---------------------------------------------------------------- optional
# transformers>=4.38     # FinBERT sentiment (falls back to the built-in lexicon)
# shap>=0.44             # exact TreeSHAP (falls back to sampled Shapley)
# lime>=0.2              # reference LIME (a native implementation ships with the app)

# --------------------------------------------------------------------- dev
pytest>=8.0
pytest-asyncio>=0.23
